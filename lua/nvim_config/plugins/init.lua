-- NB: Needs to return a table for the rest of the files to be processed as a module
return {
    -- Useful lua function specifically for Neovim
    -- {
    --     "nvim-lua/plenary.nvim",
    --     name = "plenary"
    -- },
}
